{
  "name": "动物名录生成",
  "id": "animal_catalog_generate",
  "description": "对陆生动物物种数据初步整理，输出物种名录。",
  "parameters": {
    "work_dir": "工作目录（可选）",
    "input_file": "待处理的 Excel 文件名（必填）",
    "ref_file": "参考名录文件路径（可选）",
    "pa": "居留型起始标记（可选，默认 ⅤA）",
    "regional_level": "省级保护级别（可选）"
  },
  "workflow": [
    {
      "description": "{pa}，{regional_level}，询问用户是使用默认值还是输入指定值，运行名录生成脚本",
      "tool": "run_python",
      "params": {
        "script_path": "agentskills/animal_catalog_generate.py",
        "keywords": ["--work_dir", "{work_dir}", "--input_file", "{input_file}", "--ref_file", "{ref_file}", "--pa", "{pa}", "--regional_level", "{regional_level}"]
      },
      "continue_on_error": false
    }
  ]
}

